# Implementation Runbook

- Stay within the approved plan and contract.
- Prefer isolated core modules for recommendation, persistence, and adapter logic.
- Keep UI state simple and deterministic.
- Use adapter boundaries for undocumented usage and auth sources.
- Validate with `npm run lint` and `npm run build` before claiming completion.
